# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ypnusa` / `ypnusa-com`) is currently an empty scaffold with only a `README.md`. There are no application files, dependencies, build systems, or services to run.

- **No package manager lockfiles or dependency manifests** exist yet (`package.json`, `requirements.txt`, etc.).
- **No dev server, build, lint, or test commands** are available.
- **No Docker or devcontainer configuration** is present.

Once application code is added, update this section with:
1. How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
2. How to run the dev server and any required backing services.
3. How to run lint, tests, and builds.
4. Any non-obvious environment caveats discovered during setup.
